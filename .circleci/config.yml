version: 2.1
jobs:
  docker:
    -image: circleci/node.10
  steps: 
    -checkout
    -run:
      name: install npm dependencies
      command: |
        npm install --save

workflows:
  build_tests:
    jobs:
      -run_tests