version: 2
general:
  branches:
    only:
     - master
     - test
jobs:
  build:
    context: 
            - meanstack
    docker:
      # specify the version you desire here
      - image: circleci/node:8.9.0
     

    steps:
      - checkout
      - run: npm install

      # - run: npm test
      # - run: 
      #   name: backend unit test
      # command: | 
      # sbt "backend"

 environment:
    - CIRCLE_COMPARE_URL: https://github.com/vaibhavkhatal/MEAN-Jwt-Authentication-NodeJs/compare/d853df94e316792586732e02f5a0de22f209372c...ab32f3be5ba255cf8f990c56124e516ee8af365f

  workflow:
    jobs:
    - build

   